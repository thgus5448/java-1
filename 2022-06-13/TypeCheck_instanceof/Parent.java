package TypeCheck_instanceof;

/* 객체 타입 확인(instanceof)
 * 강제 타입 변환 = 자식 타입이 부모 타입으로 변환되어 있는 상태에서만 가능.
 * 				즉, 부모 타입의 변수가 부모 객체를 참조할 경우 자식 타입으로 변환할 수 없다. 
 * 
 * 부모 변수가 참조하는 객체가 부모 객체인지 자식 객체인지 확인하는 방법은?
 * instanceof 연산자 사용(=어떤 객체가 어떤 클래스의 인스턴스인지 확인. 주로 매개값의 타입을 조사할 때 사용)
 * 	예) boolean result = 좌항(객체) instanceof 우항(타입)
 * 
 * 
 */
public class Parent {
	
}
